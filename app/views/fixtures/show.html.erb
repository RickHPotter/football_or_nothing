<% if !@show_fixture_detail && (@matchday_session&.running? || @matchday_session&.paused? || @matchday_session&.completed?) %>
  <%= render "fixtures/live_matchday_screen" %>
<% end %>

<% if @show_fixture_detail %>
  <%= render "fixtures/match_screen" %>
<% end %>
