<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Football Or Nothing" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Football Or Nothing">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="game-shell">
      <header class="game-topbar">
        <div class="game-topbar-inner">
          <div>
            <%= link_to "Football or Nothing", root_path, class: "game-brand" %>
            <% if Current.user %>
              <div class="text-xs text-board-100">
                <%= Current.user.email_address %>
              </div>
            <% end %>
          </div>

          <% if Current.user %>
            <% nav_career_id = params[:career_id].presence || (params[:id] if controller_name == "careers") %>
            <% nav_career = Current.user.careers.includes(manager: { current_manager_contract: :club }).find_by(id: nav_career_id) if nav_career_id %>
            <% nav_career ||= Current.user.careers.includes(manager: { current_manager_contract: :club }).order(created_at: :desc).first %>
            <nav class="game-nav">
              <%= game_nav_link "Home", root_path %>
              <% if nav_career %>
                <%= game_nav_link "Career", career_path(nav_career) %>
                <%= game_nav_link "Countries", career_countries_path(nav_career) %>
                <%= game_nav_link "Clubs", career_browse_clubs_path(nav_career) %>
                <%= game_nav_link "Tournaments", career_tournaments_path(nav_career) %>
                <% if nav_career.manager&.current_club %>
                  <%= game_nav_link "Club", career_club_path(nav_career) %>
                  <%= game_nav_link "Transfers", career_transfers_path(nav_career) %>
                  <%= game_nav_link "Staff", career_staff_contracts_path(nav_career) %>
                  <%= game_nav_link "Scouting", career_scouting_assignments_path(nav_career) %>
                  <%= game_nav_link "Youth", career_youth_intakes_path(nav_career) %>
                <% end %>
              <% else %>
                <%= game_nav_link "New Career", new_career_path %>
              <% end %>
              <%= button_to "Sign out", session_path, method: :delete %>
            </nav>
          <% end %>
        </div>
      </header>

      <main class="game-page">
        <% if flash[:alert] %>
          <div class="flash flash-alert"><%= flash[:alert] %></div>
        <% end %>

        <% if flash[:notice] %>
          <div class="flash flash-notice"><%= flash[:notice] %></div>
        <% end %>

        <%= yield %>
      </main>
    </div>
  </body>
</html>
