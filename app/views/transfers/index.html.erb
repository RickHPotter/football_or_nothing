<h1>Transfer Market</h1>

<p>Club: <%= @club.name %></p>

<% if @finance %>
  <p>Transfer budget: <%= number_to_currency(@finance.transfer_budget) %></p>
  <p>Available wage budget: <%= number_to_currency(@finance.available_wage_budget) %></p>
<% else %>
  <p>No financial profile exists for this club.</p>
<% end %>

<h2>Available Players</h2>

<% if @market_athletes.any? %>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Club</th>
        <th>Position</th>
        <th>Ability</th>
        <th>Fee</th>
        <th>Wage</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @market_athletes.each do |athlete| %>
        <% contract = athlete.current_athlete_contract %>
        <% suggested_fee = contract ? (contract.release_clause || (athlete.current_ability * 50_000)) : 0 %>
        <% suggested_wage = contract ? [ contract.wage, 100 ].max : 100 %>
        <tr>
          <td><%= link_to [ athlete.first_name, athlete.last_name ].join(" "), career_athlete_path(@career, athlete) %></td>
          <td><%= contract&.club&.name || "Free agent" %></td>
          <td><%= athlete.position.humanize %></td>
          <td><%= athlete.current_ability %></td>
          <td><%= number_to_currency(suggested_fee) %></td>
          <td><%= number_to_currency(suggested_wage) %></td>
          <td>
            <%= button_to "Sign", career_transfers_path(@career), params: {
              athlete_id: athlete.id,
              fee: suggested_fee,
              wage: suggested_wage
            } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>No players are currently visible in the market.</p>
<% end %>

<h2>Recent Transfers</h2>

<% if @transfers.any? %>
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th>Player</th>
        <th>From</th>
        <th>Fee</th>
        <th>Wage</th>
      </tr>
    </thead>
    <tbody>
      <% @transfers.each do |transfer| %>
        <tr>
          <td><%= transfer.transfer_date %></td>
          <td><%= transfer.athlete.first_name %> <%= transfer.athlete.last_name %></td>
          <td><%= transfer.from_club&.name || "Free agent" %></td>
          <td><%= number_to_currency(transfer.fee) %></td>
          <td><%= number_to_currency(transfer.wage) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>No incoming transfers yet.</p>
<% end %>

<%= link_to "Back to club", career_club_path(@career) %>
